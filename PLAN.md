# AI-Gateway 执行计划

## 项目目标

上游只提供纯文本。网关在文本进入供应商 LLM 前完成抽取式压缩、缓存、任务预算控制与观测。主模型不微调。项目衡量指标是相对于当前饿汉式处理基线的实际 token 减少比例，并约束客户潜力/KYC 业务质量下降不超过 10%。

## 开源优先声明

- Redis：精确缓存，不自研缓存引擎。
- Qdrant：向量索引，不自研向量数据库。
- Prometheus + Grafana：指标采集和可视化，不自研通用监控 UI。
- FastAPI + httpx + tiktoken：API、供应商调用和 token 估算。
- 当前语义候选向量是确定性的字符 n-gram 哈希，可用于功能验证，不应被当作已验证的生产 embedding。接入真实多语言 embedding 服务后再开启跨文本的直接语义复用。
- AI-Gateway 自研范围只包括客户潜力/KYC 任务策略、压缩选择、缓存准入规则、业务质量评估。

## 阶段与验收

### 阶段 1：可运行基础链路（已实现）

- 原始文本 API、句子切分、抽取式压缩、token 预算。
- Redis 精确缓存、Qdrant 语义候选缓存。
- DeepSeek OpenAI-compatible 调用。
- Grafana UI 观测 token、缓存、请求量。
- 本地真实请求和单元测试通过。

### 阶段 2：生产质量门槛

- 从真实历史记录构建去标识评测集，覆盖长短文本、中文/英文、数字、否定、反转意向、风险实体。
- 完整输入与压缩输入成对回放，统计高潜客户召回、KYC 高风险召回、误报、漏报。
- 只在业务质量下降不超过 10% 时发布相应压缩档位。
- 供应商实际 `usage` 作为账单 token 真值；本地 tokenizer 只用作预算估算。
- 接入多语言 embedding，测量语义缓存误复用率后再开放跨文本直接命中。

### 阶段 3：运营化

- 将应用容器化，与中间件一起部署；加入鉴权、限流、超时和重试。
- 将上游批量任务接入统一 API；按 task/policy/model 版本管理缓存。
- Grafana 增加业务质量面板和每日节省报表。
- 定期抽样缓存命中结果，形成阈值回归测试与自动降级。

## 当前约束

- 供应商凭据只保存在本机 `.env`，被 Git 忽略，不提交。
- 当前默认不缓存 KYC 的跨文本结果。
- 尚未接入真实业务标签，无法宣称达到 10% 质量损失上限或 70%–90% token 节省目标。
